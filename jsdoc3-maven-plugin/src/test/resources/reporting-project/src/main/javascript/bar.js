/**
 * A module which exposes a class Bar.
 *
 * @module bar
 */
require('bar', [], function(Bar) {

    /**
     * A class which does nothing.
     *
     * @class Bar
     */
    function Bar() {

    }

    return Bar;
});